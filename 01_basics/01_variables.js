// Defining Variables
// 'const' for constants, 'let' for reassigned variables, avoid 'var' due to scope issues.
/**
 * Example of defining a variable:
 * let accountName = "Jaipur"; // Declared with 'let'
 * let userState; // Uninitialized 'let' variable
 * console.log(userState); // Output: 'undefined'
 * Using console.table to display multiple values:
 * const a = 1, b = 2, c = 3, d = 4;
 * console.table([a, b, c, d]); // Displays values in a table format
 Formula: VLC
 */


const accountId =1234

accountId =23434

console.log(accountId);

console.table
